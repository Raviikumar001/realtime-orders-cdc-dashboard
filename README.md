# Apt Interview Assignment Solution

This repository is being rebuilt around a `PostgreSQL WAL/CDC + SSE + Fastify + React` architecture.

See `PLAN.md` for the approved implementation plan.

Current status:

- project structure has been reorganized into `backend/`, `frontend/`, and `db/`
- Phase 1 infrastructure files for PostgreSQL logical replication are in place
- application implementation is still in progress

"use strict";

async function start() {
  console.log("Backend scaffold ready. Phase 2 will replace this with Fastify app setup.");
}

start().catch((error) => {
  console.error(error);
  process.exit(1);
});
